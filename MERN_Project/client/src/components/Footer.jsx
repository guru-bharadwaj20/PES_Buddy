import React from 'react';

const Footer = () => (
	<footer className="site-footer">
		<div className="container text-center muted">© {new Date().getFullYear()} PES Buddy</div>
	</footer>
);

export default Footer;
